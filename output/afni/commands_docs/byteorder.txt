
=== Output of 'byteorder --help' ===

CPU byte order = LSB_FIRST

=== Output of 'byteorder -help' ===

Usage: byteorder
Prints out a string indicating the byte order of the CPU on
which the program is running.  For this computer, we have:

CPU byte order = LSB_FIRST

=== Output of 'byteorder' (no arguments) ===

CPU byte order = LSB_FIRST
