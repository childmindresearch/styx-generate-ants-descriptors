
=== Output of 'AntsDenoiseImageFs --help' ===

				Help

NAME
	AntsDenoiseImageFs

SYNOPSIS
	AntsDenoiseImageFs [options] -i <invol> -o <outvol>

DESCRIPTION
	Denoises an image with a spatially adaptive filter. This programs 
	wraps the AntsDenoiseImage utility available in the ANTs package (see 
	http://stnava.github.io/ANTs).

REQUIRED FLAGGED ARGUMENTS
	-i, --input invol
		input volume file

	-o, --output outvol
		denoised volume file

OPTIONAL FLAGGED ARGUMENTS
	--rician
		Enable rician noise model (otherwise gaussian is used)



=== Output of 'AntsDenoiseImageFs -help' ===

error: unexpected argument '-help'

=== Output of 'AntsDenoiseImageFs' (no arguments) ===

				Help

NAME
	AntsDenoiseImageFs

SYNOPSIS
	AntsDenoiseImageFs [options] -i <invol> -o <outvol>

DESCRIPTION
	Denoises an image with a spatially adaptive filter. This programs 
	wraps the AntsDenoiseImage utility available in the ANTs package (see 
	http://stnava.github.io/ANTs).

REQUIRED FLAGGED ARGUMENTS
	-i, --input invol
		input volume file

	-o, --output outvol
		denoised volume file

OPTIONAL FLAGGED ARGUMENTS
	--rician
		Enable rician noise model (otherwise gaussian is used)


error: missing required input '--input'
