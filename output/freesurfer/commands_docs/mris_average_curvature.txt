
=== Output of 'mris_average_curvature --help' ===

usage: /usr/local/freesurfer/bin/mris_average_curvature [options] <input curv. file> <hemi> <surface> <subject> ...  <output curv file >
the output curvature file will be painted onto the last subject name specified
on the command line.
if the -s flag is specified then the last parameter specifies
the directory in which to write the statistical maps.
if the -o flag is specified then it overrides the last subject
as the output surface.

This program will add a template into an average surface.

valid options are:

-s <cond #>     generate summary statistics and write
                them into sigavg<cond #>-<hemi>.w and
                sigvar<cond #>-<hemi>.w.

=== Output of 'mris_average_curvature -help' ===

unknown option -help

=== Output of 'mris_average_curvature' (no arguments) ===

usage: /usr/local/freesurfer/bin/mris_average_curvature [options] <input curv. file> <hemi> <surface> <subject> ...  <output curv file >
the output curvature file will be painted onto the last subject name specified
on the command line.
if the -s flag is specified then the last parameter specifies
the directory in which to write the statistical maps.
if the -o flag is specified then it overrides the last subject
as the output surface.
