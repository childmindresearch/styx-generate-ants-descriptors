
=== Output of 'dmri_spline --help' ===


USAGE: /usr/local/freesurfer/bin/dmri_spline

Basic inputs
   --cpts <file>:
     Input text file containing control points
   --mask <file>:
     Input mask volume (spline is not allowed to stray off mask)

Outputs (at least one output type must be specified)
   --out <file>:
     Output volume of the interpolated spline
   --show:
     Highlight control points in output volume (default: no)
   --outpts <file>:
     Output text file containing all interpolated spline points
   --outvec <base>:
     Base name of output text files containing tangent vectors,
     normal vectors, and curvatures at every point along the
     spline (both analytical and finite-difference versions)

Other options
   --debug:     turn on debugging
   --checkopts: don't run anything, just check options and exit
   --help:      print out information on how to use this program
   --version:   print out version and exit


...


=== Output of 'dmri_spline -help' ===

ERROR: Option -help unknown
       Did you really mean --help ?

=== Output of 'dmri_spline' (no arguments) ===


USAGE: /usr/local/freesurfer/bin/dmri_spline

Basic inputs
   --cpts <file>:
     Input text file containing control points
   --mask <file>:
     Input mask volume (spline is not allowed to stray off mask)

Outputs (at least one output type must be specified)
   --out <file>:
     Output volume of the interpolated spline
   --show:
     Highlight control points in output volume (default: no)
   --outpts <file>:
     Output text file containing all interpolated spline points
   --outvec <base>:
     Base name of output text files containing tangent vectors,
     normal vectors, and curvatures at every point along the
     spline (both analytical and finite-difference versions)

Other options
   --debug:     turn on debugging
   --checkopts: don't run anything, just check options and exit
   --help:      print out information on how to use this program
   --version:   print out version and exit

